# Site configuration

site:
  title: "My thoughts"
  tagline: "A personal blog"
  description: "Jotting down some thoughts as i go"
  
# Display settings  
display:
  articles_per_page: 20
  recent_count: 10
  
# Build settings
build:
  output_dir: "site/dist"
  search_index: "search-index.json"
